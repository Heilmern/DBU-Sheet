name: dbu_sheet
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.2+3

environment:
  sdk: ^3.12.2

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

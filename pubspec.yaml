name: dbu_sheet
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.3+4

environment:
  sdk: ^3.12.2

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
